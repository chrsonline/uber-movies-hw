<?php

use Illuminate\Foundation\Testing\WithoutMiddleware;
use Illuminate\Foundation\Testing\DatabaseMigrations;
use Illuminate\Foundation\Testing\DatabaseTransactions;

class FilmingLocationTest extends TestCase
{
  /**
   * Test that filming locations can be inserted.
   *
   * @return void
   */
  public function testRecordInsertion()
  {
    // -- For brevity these are not implemented --

    // Seed the database
    // Run insert operation
    // Verify insertion
    // Delete old data
    $this->assertTrue(true);
  }

  /**
   * Test that geocoding location information can be attached to a location record.
   *
   * @return void
   */
  public function testAttachGeocode()
  {
    // -- For brevity these are not implemented --

    // Seed the database with a location
    // Fetch and update relational data
    // Verify insertion / updates
    // Delete mocked/seeded data
    $this->assertTrue(true);
  }
}
